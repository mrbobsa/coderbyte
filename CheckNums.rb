def CheckNums(num_one,num_two)

  return -1 if num_one === num_two
  return num_one < num_two
end
   
# keep this function call here 
# to see how to enter arguments in Ruby scroll down   
CheckNums(STDIN.gets)  
